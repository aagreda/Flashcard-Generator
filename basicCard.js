//set module.exports equal to the basic flashcard variable
module.exports = BasicFlashCard;

//create the Basic flashcard variable 
var BasicFlashCard = function(front, back) {
    this.front = front;
    this.back = back;
};